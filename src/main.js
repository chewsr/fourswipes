import React from 'react'
import ReactDOM from 'react-dom'
import AppContainer from './containers/AppContainer'

ReactDOM.render(<AppContainer />, document.getElementById('main-container'))

export let __hotReload = true
